- hosts: all
  gather_facts: no
  tasks:
    - name: Run IPv6 Monitor Script
      command: python3 collect_ipv6_new.py
      args:
        chdir: "/opt"  # 修正：直接写路径字符串，不要加 {{ }}
      register: result
      
    - name: Print Output
      debug: 
        var: result.stdout_lines